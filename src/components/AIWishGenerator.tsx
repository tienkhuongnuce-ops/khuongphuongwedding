
import React from 'react';

// This component is currently disabled.
// We remove all imports to prevent build errors with geminiService.
const AIWishGenerator: React.FC = () => {
  return null;
};

export default AIWishGenerator;
